#ifndef MATHX_H
#define MATHX_H

/* Tiny versioned library used to exercise cidx component version detection
 * (the trailing path segment "1.2.0" should become the component version). */
int mathx_add(int a, int b);
int mathx_square(int n);

#endif /* MATHX_H */
